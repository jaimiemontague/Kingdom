You are being activated for Round 2 of the WK18 Sprint (LLM-AI Merger, Physicality, & Dev Tools).

Read your assignment in the PM hub:
.cursor/plans/agent_logs/agent_01_ExecutiveProducer_PM.json
→ sprints["wk18-llm-merger-and-mechanics"].rounds["wk18_r1_kickoff"] -> pm_next_actions_by_agent["03"]
(Note: it's still under the r1_kickoff round block, but your assignment is listed there).

Specifically, you need to:
1. Implement soft collision boxes / flocking separation for heroes in the engine so they no longer clump exactly on top of each other.
2. Hook Agent 06's LLM tools (`leave_building`, `move_to`, etc.) from the Behavior system directly into the physical Engine state machine so that when the LLM issues an action intent, the hero literally executes it in the physical simulation immediately.

After completing your work:
1. Update your agent log
2. Run: python tools/qa_smoke.py --quick (must PASS)
3. Report status back