# FULL FINDING INVENTORY (187)


## engine-core (9)
- [high/redundant] GameEngine._maybe_apply_early_pacing_nudge + _nearest_lair_to are DEAD duplicates of the live SimEngine versions (and a test monkeypatches the dead copy) — game/engine.py:1103-1119 (_nearest_lair_to), game/engine.py:1121-1180 (_maybe_apply_early_pacing_nudge) -> Delete GameEngine._maybe_apply_early_pacing_nudge (engine.py:1121-1180) and GameEngine._nearest_lair_to (engine.py:1103-1119) entirely. Update tests/test_engine
- [high/oversized_file] engine.py is a 1735-LOC god-object mixing input/selection, a cheat console, sim-action mutators, lifecycle, and HUD action routing — game/engine.py:1-1735, game/engine.py:67-289 (__init__, 222 LOC) -> Extract three more facade collaborators following the existing engine_facades pattern, plus move the console out. Target: engine.py shrinks to ~700 LOC of compo
- [high/redundant] 35 repetitions of the 'set one selection, null the other four' idiom across try_select_* — fragile and already caused a shipped bug — game/engine.py:735-955 (try_select_hero/hero_at_world/tax_collector/guard/peasant/enemy/building), game/engine.py:860-915 (try_ursina_select_unit_at_screen — repeats the 5-line clear block five times, once per kind) -> Add a single SelectionState.select_exclusive(kind, entity_id) (or an engine-side _select_only(kind, entity) helper) that sets the chosen slot and clears all oth
- [medium/dead_code] GameEngine._build_system_context is dead and returns a STALE 6-field SystemContext (sim's is the live 13-field one) — game/engine.py:1285-1294, game/sim_engine.py:354-373 (live, 13 fields) -> Delete GameEngine._build_system_context (engine.py:1285-1294). SystemContext construction is sim-owned; the presentation layer has no business building it.
- [medium/boundary_leak] ~20 ad-hoc `_ursina_*` attributes form an untyped getattr/setattr contract spanning engine<->input<->ursina_app<->renderers — game/engine.py:102 (_ursina_pointer_world_sim init), game/engine.py:293 (_request_ursina_hud_upload setattr) -> Introduce a small typed UrsinaViewerState dataclass owned by GameEngine (self.ursina: UrsinaViewerState | None = None). Fields: active: bool, skip_world_render:
- [medium/poor_design] apply_hud_pin_action is a stringly-typed 11-branch dispatcher that also mutates sim state (building destruction) from the HUD-routing layer — game/engine.py:1440-1522, game/engine.py:1475-1488 (confirm_demolish: sets building.hp=0 and calls _cleanup_destroyed_buildings) -> Move this method to the new actions facade. Split it: (a) demolition (confirm_demolish) becomes an explicit EngineActions.demolish_building(building) that is th
- [medium/over_complex] process_command: a 111-LOC cheat/chat console hard-coded inside GameEngine — game/engine.py:323-434, game/graphics/ursina_app.py:1408 (calls engine.process_command('/revealmap')) -> Extract to game/console.py as a registry of command handlers: a dict {name: (handler, help)} where each handler takes (engine, arg). GameEngine.process_command 
- [medium/over_complex] GameEngine.__init__ is 222 LOC with three near-duplicate display/UI bootstrap branches and an inline _NullStub class — game/engine.py:67-289, game/engine.py:135-199 (3-way display surface branch) -> Split __init__ into focused private helpers: _init_perf_counters(), _init_display_surfaces() (one body parameterized by a (size, srcalpha, use_display_manager) 
- [low/perf] CleanupManager iterates every hero/enemy/peasant/guard/bounty list to null references on each building destroy (O(buildings x all-entities)); reference-clearing also duplicated with SimEngine cleanup — game/cleanup_manager.py:38-115, game/cleanup_manager.py:77-101 (5 separate full-list scans per destroyed building) -> Make SimEngine the single owner of destruction+reference-clearing and have on-demand demolish call into it (e.g. sim.destroy_building(building, emit_messages)) 

## sim-engine (13)
- [high/boundary_leak] SimEngine.update() takes the presentation game_state dict only to read castle + forward to entities — leaky sim<->ui boundary — game/sim_engine.py:623, game/sim_engine.py:692 -> Stop threading the UI dict into the sim. (1) Compute castle inside the sim once (cache it, see separate finding) instead of reading game_state['castle']. (2) De
- [high/oversized_file] sim_engine.py is a 1331-LOC god module mixing world-gen, tree/lumber economy, fog-of-war, entity separation, building dispatch, cleanup, pacing, and snapshot assembly — game/sim_engine.py:1, game/sim_engine.py:155-212 -> Extract four pure modules under game/sim/ and keep SimEngine as a thin orchestrator that owns state and delegates. Each extracted function takes explicit inputs
- [high/over_complex] _update_fog_of_war is 148 lines doing 6 things with 5 inline vision-radius constant sets and two near-identical building-iteration passes — game/sim_engine.py:1183-1331, game/sim_engine.py:1204-1209 -> When extracting to game/sim/fog.py (previous finding), also (a) hoist all vision radii into config or a single VISION_TILES dict so there is one source of truth
- [medium/dead_code] SimEngine.selected_* attributes are dead compat stubs read by a dead code path in get_game_state() — game/sim_engine.py:137-140, game/sim_engine.py:424 -> Delete the four self.selected_* stubs from SimEngine.__init__ and remove all selection reads from SimEngine.get_game_state(): emit selected_hero/selected_buildi
- [medium/dead_code] SystemRunner is constructed but never used on the hot path — update() calls buff_system/wave_event_system directly, so the ordered runner is dead wiring — game/sim_engine.py:130, game/sim_engine.py:691 -> Either (a) delete _ordered_systems and the SystemRunner class until there is a contiguous run of fire-and-forget systems to host (honest: removes dead wiring), 
- [medium/perf] castle is looked up via the same `next(... building_type=='castle')` scan 4x per tick across sim methods — game/sim_engine.py:355, game/sim_engine.py:406 -> Cache the castle as self._castle when it is created in setup_initial_state (line 268-273) and expose a _castle() accessor that returns the cached ref, re-resolv
- [medium/redundant] Lair-clear bounty claiming is duplicated and inverted vs BountySystem.check_claims — sim re-implements claim resolution inline — game/sim_engine.py:1083-1120, game/sim_engine.py:778-794 -> Move typed-bounty completion into BountySystem (e.g. BountySystem.claim_target(hero, target, bounty_type) -> list[(bounty,hero)]) so both proximity and target-b
- [medium/stringly_typed] Pervasive defensive getattr/duck-typing on the sim's own concrete entity types hides the real data model — game/sim_engine.py:185-188, game/sim_engine.py:543-546 -> For the sim's own types, access attributes directly (hero.x, building.center_x, tree.grid_x). Reserve getattr-with-default for genuinely optional/cross-boundary
- [medium/stringly_typed] get_game_state returns a 30-key raw dict mixing sim truth, UI layout, and a self-reference — stringly-typed cross-layer contract — game/sim_engine.py:375-443, game/sim_engine.py:434 -> Split the bag along its real seams: (1) keep a typed SimReadModel for sim truth (or reuse the already-typed SimStateSnapshot for renderer-facing data), (2) put 
- [low/perf] get_unclaimed_bounties() is recomputed (new list allocation) 3-4x per frame across sim read-models — game/sim_engine.py:368, game/sim_engine.py:421 -> Compute unclaimed once per tick. Lowest-risk: in update(), after bounty_system.cleanup(), cache self._unclaimed_bounties = self.bounty_system.get_unclaimed_boun
- [low/poor_design] _update_buildings dispatches by string building_type through a long if/elif chain with duplicated guard/palace spawn logic — game/sim_engine.py:959-1002, game/sim_engine.py:981-995 -> Give buildings a uniform tick contract so the sim doesn't switch on type. Either (a) each building exposes update(self, ctx) reading what it needs from a System
- [low/boundary_leak] sim/ package conflates pure deterministic primitives with an LLM/AI glue cluster that imports ai.* — game/sim/direct_prompt_exec.py:12-13, game/sim/direct_prompt_targets.py:129 -> Relocate the direct-prompt trio (direct_prompt_exec/targets/commit) and hero_guardrails_tunables under a dedicated boundary package, e.g. game/sim/llm_bridge/ o
- [low/perf] Tree<->tile reconciliation in update() is inline, allocates two full sets, and rebuilds the entire growth lookup every tick — game/sim_engine.py:643-667, game/sim_engine.py:654-657 -> Have NatureSystem.tick return the list of newly-spawned saplings (or mutate a passed-in 'spawned' list), and update only those entries in _tree_growth_by_tile +

## input-commands (game/input_handler.py, game/game_commands.py) (8)
- [high/oversized_file] input_handler.py is an oversized 772-LOC god-file mixing 6 unrelated input concerns — game/input_handler.py:1-772 -> Split into a small package game/input/ with InputHandler as a thin router that owns the event loop and delegates to focused handler classes, each constructed wi
- [high/redundant] Selection mutual-exclusion is implemented THREE times (SelectionState + engine try_select_* + handle_mousedown cascade) — game/input_handler.py:653-684, game/engine.py:754-955 -> Make each engine try_select_* fully own its post-selection invariant (including building_panel.deselect()) and have handle_mousedown's success branches become s
- [high/parallel_registry] Building hotkey registry is triplicated and already drift-prone (if/elif chain vs BUILDING_HOTKEYS vs placeable_buildings) — game/input_handler.py:298-320, game/ui/build_catalog_panel.py:21-32 -> Promote a single ordered registry to config.py and derive both the input mapping and the catalog UI from it. Replace the if/elif ladder with a dict lookup.
- [medium/stringly_typed] handle_mousedown decodes ~20 magic-string HUD actions in a 90-line if/elif ladder (stringly-typed parallel command bus) — game/input_handler.py:451-535 -> Extract a HudActionDispatcher with an explicit table mapping action-name -> handler(c, action). No-op consume-only actions map to a shared _consume handler. Rep
- [medium/over_complex] EngineCommandHub: ~40 hand-written one-line pass-through properties, all typed Any — boilerplate that re-hides the types WK63 was meant to add — game/game_commands.py:195-512 -> (1) For the pure pass-throughs that carry no logic, EngineCommandHub adds nothing over passing the engine directly — but since InputHandler is the only consumer
- [medium/poor_design] Command-mode text-entry state lives on GameEngine and is duplicated by a dead toggle_command_mode method — game/input_handler.py:179-206, game/engine.py:110-111 -> Move the command-line buffer into a CommandModeInput object owned by the input router; expose only process_command(cmd) on the command surface. Delete GameEngin
- [medium/poor_design] Pervasive getattr/hasattr defensive probing of a hub that already guarantees the attributes — game/input_handler.py:118-119, game/input_handler.py:211-230 -> Since the hub guarantees the surface, access attributes directly and use a single small helper for the genuinely-optional sub-objects (e.g. hud._chat_panel whic
- [low/dead_code] Five identical typed command aliases are stored but never used; every method re-binds c=self.commands — game/input_handler.py:30-41 -> Either (a) actually route calls through the typed aliases (c.selection.try_select_hero, c.placement.place_building, etc.) so the narrow Protocols earn their kee

## entities-units (9)
- [high/redundant] Four entities copy-paste the same path-replan-and-follow block — game/entities/enemy.py:289-320, game/entities/enemy.py:562-589 -> Extract a single helper in game/systems/navigation.py: `advance_along_path_to(entity, world, buildings, goal_x, goal_y, dt, *, commit_ms=500, replan_ok_ms=150, 
- [high/oversized_file] Hero is 1152 LOC mixing 7 responsibilities — split into focused modules — game/entities/hero.py:50-1152 -> Keep Hero as a thin state container + property accessors and move cohesive clusters into mixins or (better) plain helper modules that operate on a hero. Lowest-
- [high/parallel_registry] Two parallel implementations of hero intent taxonomy (entity vs AI) — game/entities/hero.py:1013-1086, ai/basic_ai.py:144-175 -> Make ai/ the single owner of intent derivation. Move _derive_intent into ai/ (e.g. ai/intent.py:derive_intent(hero) returning (intent, reason, ctx)) keyed off t
- [medium/dead_code] Dead/duplicate attribute and method definitions in Hero.__init__ — game/entities/hero.py:74, game/entities/hero.py:130 -> Delete the duplicate `self._event_bus` (line 130), delete the untyped intent/last_decision pair (lines 141-142) keeping only the typed pair (152-153), and delet
- [medium/redundant] Inn loiter-fee economy duplicated verbatim in two Hero methods — game/entities/hero.py:517-531, game/entities/hero.py:868-883 -> Extract `def _apply_inn_loiter_fee(self, building, dt) -> bool` on Hero (returns True if ejected) and call it from both sites. When Hero is split (see oversized
- [medium/over_complex] Enemy roster is 7 near-identical 12-line stat-block subclasses — game/entities/enemy.py:382-414, game/entities/enemy.py:592-668 -> (a) Move register_attacker/attackers up to the Enemy base class so it exists uniformly and drop the hasattr guard in combat.py. (b) Replace the 7 stat subclasse
- [medium/over_complex] SkeletonArcher.update is a 130-line near-duplicate of Enemy.update + id()-based determinism risk — game/entities/enemy.py:460-589, game/entities/enemy.py:479 -> After extracting advance_along_path_to (finding 1), SkeletonArcher.update shrinks to: tick cooldowns, retarget, then a small kiting decision that calls the shar
- [low/perf] Hero.attack/defense recompute buff totals on every property access (per-combat-event waste) — game/entities/hero.py:243-263, game/systems/combat.py:156 -> Drop the getattr indirection (use b.expires_at_ms / b.atk_delta directly since Buff is a dataclass with those fields) and short-circuit when self.buffs is empty
- [low/poor_design] Hero.get_context_for_llm and _derive_intent re-scan all enemies/buildings each call (no reuse of nav helpers) — game/entities/hero.py:1098-1151, game/entities/hero.py:1029-1030 -> Move LLM-context assembly out of Hero into the ai/ layer (it is an AI concern, mirrors the intent boundary leak) and have it consume a pre-filtered nearby-entit

## buildings (8)
- [high/boundary_leak] Stringly-typed building update() dispatch ladder with 6 divergent signatures — C:\Users\Jaimie Montague\OneDrive\Documents\Kingdom\game\sim_engine.py:970-995, C:\Users\Jaimie Montague\OneDrive\Documents\Kingdom\game\entities\buildings\economic.py:94 -> Introduce a uniform tick contract: define a SystemContext-like BuildingTickContext (or reuse the existing widened SystemContext from WK64) carrying economy, ene
- [high/redundant] Three near-duplicate ranged-tower update() bodies; dead get_ranged_spec interface and dead _last_ranged_events list — C:\Users\Jaimie Montague\OneDrive\Documents\Kingdom\game\entities\buildings\defensive.py:39-101, C:\Users\Jaimie Montague\OneDrive\Documents\Kingdom\game\entities\buildings\defensive.py:125-183 -> Extract a RangedAttackMixin (or free helpers in a new defensive_combat.py) providing find_nearest_enemy_in_range(enemies, range) and make_projectile_event(to_x,
- [high/redundant] Timed-research state machine duplicated across Marketplace/Blacksmith/Library; base research_progress_0_to_1 is dead — C:\Users\Jaimie Montague\OneDrive\Documents\Kingdom\game\entities\buildings\economic.py:63-92, C:\Users\Jaimie Montague\OneDrive\Documents\Kingdom\game\entities\buildings\economic.py:147-200 -> Add a ResearchableMixin (new module researchable_mixin.py) that owns the entire timed-research lifecycle: start_research(key, cost, on_complete), advance_resear
- [medium/oversized_file] base.py mixes 4 unrelated responsibilities (ID allocator, global research registry, geometry helper, Building) in one file — C:\Users\Jaimie Montague\OneDrive\Documents\Kingdom\game\entities\buildings\base.py:1-312 -> Split base.py into: ids.py (id allocation), research_state.py (RESEARCH_UNLOCKS + helpers — and ideally make it an injectable object rather than module global, 
- [medium/parallel_registry] Six parallel string-keyed registries define one building; factory + config tables must be hand-synced — C:\Users\Jaimie Montague\OneDrive\Documents\Kingdom\game\entities\buildings\types.py:8-38, C:\Users\Jaimie Montague\OneDrive\Documents\Kingdom\game\building_factory.py:45-73 -> Introduce a single BUILDING_SPECS table (one row per building: cls, size, color, cost, max_occupants) as the single source of truth, derive BuildingFactory.BUIL
- [medium/error_handling] Defensive-barnacle code: TypeError-arity cascade and pervasive getattr on always-present attributes — C:\Users\Jaimie Montague\OneDrive\Documents\Kingdom\game\ui\building_panel.py:396-404, C:\Users\Jaimie Montague\OneDrive\Documents\Kingdom\game\entities\buildings\base.py:179-184 -> Define a typed Researchable Protocol (research(name, economy, game_state=None)->bool, advance_research, research_progress) and have UI depend on it; replace the
- [medium/poor_design] Library.advance_research reaches across all buildings via stashed raw-dict game_state to re-tune other entities — C:\Users\Jaimie Montague\OneDrive\Documents\Kingdom\game\entities\buildings\special.py:86, C:\Users\Jaimie Montague\OneDrive\Documents\Kingdom\game\entities\buildings\special.py:90-141 -> Emit a research_completed event (via the existing EventBus / SystemContext) instead of stashing game_state; let a small ResearchEffects system (or the buff/econ
- [low/dead_code] Inn backward-compat alias properties and dead heroes_resting/gold_earned_from_drinks shims — C:\Users\Jaimie Montague\OneDrive\Documents\Kingdom\game\entities\buildings\economic.py:237-250, C:\Users\Jaimie Montague\OneDrive\Documents\Kingdom\game\entities\buildings\economic.py:220-226 -> Grep for external readers of heroes_resting / gold_earned_from_drinks; if confined to one panel, migrate that reader to occupants/drink_income_gold and delete t

## game/systems (12)
- [high/redundant] Triplicated difficulty HP/damage scaling across spawner, lairs, and wave_events — game/systems/spawner.py:189-197, game/systems/lairs.py:144-153 -> Add one method to DifficultySystem: `apply_to_enemy(enemy)` (and optionally `apply_to_enemies(iterable)`) that performs the hp/damage scaling once. Replace all 
- [high/oversized_file] poi_interaction.py is oversized and mixes proximity-scan, dispatch, 7 handlers, and dead underground code — game/systems/poi_interaction.py:1-503, game/systems/poi_interaction.py:445-491 -> Split into a focused package game/systems/poi/. Keep the scanner+cooldown engine thin; move each handler family into a handlers module; delete the dead undergro
- [high/stringly_typed] Per-hero/per-POI cooldown keyed on Python id() instead of stable entity_id/hero_id — game/systems/poi_interaction.py:113, game/systems/poi_interaction.py:121-128 -> Key cooldowns on `(hero.hero_id, poi.entity_id)`. This makes the cooldown map serializable and consistent with the rest of the sim, with no behavioral change in
- [medium/redundant] POI world-center math reimplemented 6+ times instead of using Building.center_x/center_y — game/systems/poi_interaction.py:100-101, game/systems/poi_interaction.py:228-229 -> Add a single helper (or reuse Building.center_x/center_y) and call it everywhere. Optionally fold _check_poi_discovery into POIInteractionSystem so discovery + 
- [medium/redundant] CombatSystem has two parallel public entrypoints and dead query helpers — game/systems/combat.py:82-98, game/systems/combat.py:100-106 -> Keep _run_combat + the protocol update(); make process_combat a 2-line shim that calls update() with a throwaway ctx OR keep it but document it's tooling-only. 
- [medium/redundant] poi_placement.py has two near-duplicate spot-finders (zone vs frontier) — game/systems/poi_placement.py:241-304, game/systems/poi_placement.py:350-406 -> Extract one `_find_spot(world, definition, placed, structures, castle_cx, castle_cy, rng, *, accept_zone, sigma_frac, min_castle_dist=0, max_attempts)` where `a
- [medium/dead_code] LayerPathfinder and underground A* are dormant dead weight inside pathfinding.py — game/systems/pathfinding.py:218-368, game/sim_engine.py:323-346 -> Move LayerPathfinder + underground A* out of the hot pathfinding.py into game/systems/layer_pathfinding.py (clear it's an optional/experimental subsystem), or d
- [medium/boundary_leak] Bounty object carries presentation/cache state (parallel contract + ui_ mirrors) — game/systems/bounty.py:38-58, game/systems/bounty.py:338-351 -> (1) Remove the cached Surface fields from Bounty; cache text surfaces in the HUD renderer keyed by bounty_id. (2) Collapse responders/responder_count and ui_* i
- [medium/over_complex] neutral_buildings: O(w*h*ow*oh) footprint-gap check and an opaque nested-closure type picker — game/systems/neutral_buildings.py:36-49, game/systems/neutral_buildings.py:52-85 -> (1) Replace _min_chebyshev_between_footprints with the rectangle-gap formula (O(1)). (2) Replace _MarketProxy with a center-point param to _find_spot. (3) Lift 
- [low/perf] find_path returns wrong distance budget after goal relocation; redundant blocked-cache rebuild — game/systems/pathfinding.py:112-131, game/systems/navigation.py:88-89 -> Compute dist AFTER goal relocation. Replace the `==8000` sentinel with `max_expansions: int | None = None` and derive the default inside. Have navigation reuse 
- [low/error_handling] check_interactions never sets per-hero cooldown for one-shot handlers when they early-return — game/systems/poi_interaction.py:124-128, game/systems/poi_interaction.py:206-212 -> In _resolve (or check_interactions), stamp a long cooldown whenever a handler no-ops on an already-interacted POI, so lingering heroes stop re-dispatching. Simp
- [low/error_handling] _emit_event swallows all exceptions silently across POI and combat — game/systems/poi_interaction.py:430-438, game/systems/poi_interaction.py:148-152 -> Let event emission raise in dev (it's the sim's own bus), or at minimum log via the existing logging facility instead of pass. Remove the try/except around the 

## world (8)
- [high/oversized_file] World is a God-class mixing tile data, world-gen, fog-of-war, AND pygame rendering — game/world.py:44, game/world.py:79 -> Split `World` into a thin tile-data core plus three collaborators by responsibility (see oversized_files blueprint). Move generate_terrain/generate_heightmap in
- [high/stringly_typed] _currently_visible has a contradictory type contract (list in sim, set in renderer/tools) — latent crash — game/world.py:66, game/world.py:409 -> Make `_currently_visible` a `set[tuple[int,int]]` of record (it is semantically a set of tiles visible this frame). Use `.add()` in _reveal_circle, `.clear()` i
- [medium/redundant] Tree-growth blocking logic is duplicated verbatim in is_walkable and is_buildable with getattr+try/except boilerplate — game/world.py:322-331, game/world.py:341-350 -> Extract one private helper `_tree_blocks(self, x, y) -> bool` and a module constant `TREE_BLOCK_GROWTH = 0.75`. Set `self.tree_growth_lookup = lambda _x,_y: 1.0
- [medium/perf] fog_disabled full-map reveal rescans the entire grid every tick instead of early-returning — game/world.py:447-456, game/sim_engine.py:1197 -> Make the fog-disabled reveal idempotent and O(1) after the first pass: track a `_revealed_all` flag. On the first fog_disabled call, fill the grid once and set 
- [medium/over_complex] generate_heightmap is a 110-line monolith doing 5 distinct passes inline — game/world.py:173-282 -> When extracting to game/worldgen.py (finding #1), split this into named pure helpers: `_perlin_base(gw,gh,params)`, `_apply_castle_plateau(hmap,...)`, `_clamp_w
- [medium/boundary_leak] check_underground_hero_retreat is AI/sim logic wrongly living in the underground data module, reached entirely through getattr/hasattr duck-typing — game/underground.py:139-173, game/sim_engine.py:684-687 -> Move check_underground_hero_retreat to ai/behaviors/ (next to the other behavior modules that already import from game.world) or convert it to a Hero method `he
- [low/redundant] get_zone and get_zone_blend duplicate the entire zone-resolution walk — game/world_zones.py:156-192, game/world_zones.py:195-238 -> Implement get_zone in terms of get_zone_blend (the blend version is a strict superset): `def get_zone(...): return get_zone_blend(...)[0]`. This deletes ~35 dup
- [low/parallel_registry] Heightmap stored in two places: World.heightmap and the global terrain_height module — game/world.py:58-61, game/world.py:282 -> Make terrain_height the single owner of the displayed heightmap and have it hold a reference (not a copy) to World.heightmap, OR have World call terrain_height.

## ai-core (9)
- [high/dead_code] Dead legacy prompt system: SYSTEM_PROMPT + build_decision_prompt + build_summary are unreachable in production — ai/prompt_templates.py:27 (SYSTEM_PROMPT), ai/prompt_templates.py:106 (DECISION_PROMPT) -> Delete the legacy autonomous prompt path. Remove build_summary from context_builder.py, remove DECISION_PROMPT/build_decision_prompt/SYSTEM_PROMPT from prompt_t
- [high/over_complex] validate_direct_prompt_output is a 295-line single function — split into a per-intent handler table — ai/direct_prompt_validator.py:140-434 (validate_direct_prompt_output) -> Extract each intent into a small pure handler `(state) -> Resolution` and dispatch via a dict keyed by intent. Introduce a tiny mutable dataclass (or TypedDict)
- [high/oversized_file] ContextBuilder.build_hero_context is a 227-line god-method with inline imports and getattr soup — ai/context_builder.py:71-298 (build_hero_context), ai/context_builder.py:171 (inline import poi_awareness) -> Split build_hero_context into focused private builders and assemble. This also removes the awkward double call to select_known_places_for_llm by computing the p
- [high/oversized_file] BasicAI mixes coordinator role with ~250 LOC of inline state-machine handlers — ai/basic_ai.py:367-427 (handle_fighting), ai/basic_ai.py:429-452 (handle_retreating) -> Move these into ai/behaviors to match the existing seam, leaving BasicAI as a thin dispatcher (state_handlers dict). combat -> ai/behaviors/combat.py (handle_fi
- [medium/dead_code] Fully dead conversation prompt builder superseded by prompt_packs — ai/prompt_templates.py:72-93 (CONVERSATION_SYSTEM_PROMPT), ai/prompt_templates.py:95-103 (CONVERSATION_USER_PROMPT) -> Delete CONVERSATION_SYSTEM_PROMPT, CONVERSATION_USER_PROMPT, and build_conversation_prompt outright. No import sites exist.
- [medium/stringly_typed] Action/intent/place vocabulary is stringly-typed and duplicated across 5 files (TargetType already exists for tasks) — ai/prompt_templates.py:5-24 (VALID_ACTIONS, TOOL_ACTIONS), ai/direct_prompt_validator.py:15-53 (SUPPORTED_DIRECT_INTENTS, DEFERRED_COMBAT_INTENTS, _PLACE_TYPE_TO_MOVE_TARGET, _PLAYER_HOME_TYPES) -> Add a single ai/vocab.py defining ToolAction(str, Enum) and DirectIntent(str, Enum) (values = current strings), plus the canonical building/place-type groupings
- [medium/perf] Per-decision recompute of full hero context (build_hero_context called 2-3x per consult) — ai/behaviors/llm_bridge.py:71 (request path build #1), ai/profile_context_adapter.py:101 (_compact_situation builds full context AGAIN) -> Build the full context once per consult and pass it into build_llm_context_for_moment instead of having the adapter rebuild it. Thread the already-built base_co
- [low/boundary_leak] refresh_intent prefers a private hero hook via hasattr, with a dead duplicate fallback — ai/basic_ai.py:144-175 (refresh_intent), ai/basic_ai.py:152 (hasattr(hero,'_update_intent_and_decision')) -> Make intent derivation a public hero method (e.g. hero.update_intent(game_state)) and call it unconditionally, deleting the fallback. If older hero versions tru
- [low/redundant] _process_request and _process_conversation duplicate event-emit + JSON-extraction boilerplate — ai/llm_brain.py:165-188 (decision emit pair), ai/llm_brain.py:212-235 (autonomous emit pair) -> Extract two helpers: _complete_with_events(hero_key, system_prompt, user_prompt, mode, timeout) -> response_text (wraps the two emits + provider.complete), and 

## ai-behaviors (ai/behaviors/*.py + ai/providers/*.py, plus ai/arrival_handlers.py & ai/contracts.py) (13)
- [high/poor_design] bounty_pursuit.handle_moving is the de-facto global MOVING-state dispatcher, not bounty code — ai/behaviors/bounty_pursuit.py:223-364, ai/basic_ai.py:364-365 -> Rename the dispatcher and split it. Create ai/behaviors/movement.py owning handle_moving as a thin orchestrator that calls, in order: bounty_pursuit.update_boun
- [high/oversized_file] exploration.handle_idle is a 160-line god-function with 4 inlined copies of 'route hero to a building' — ai/behaviors/exploration.py:190-351, ai/behaviors/exploration.py:216-231 -> Extract one helper `route_to_building(hero, world, buildings, building) -> None` (sets target_position to adjacent-or-center) and reuse it everywhere (also fixe
- [high/redundant] 'Route hero to adjacent-or-center tile of a building' is duplicated 11 times verbatim — ai/behaviors/exploration.py:218-228, ai/behaviors/exploration.py:241-251 -> Add a single helper and call it everywhere. Put it in a new ai/behaviors/movement.py (or navigation_helpers.py) so both behaviors and basic_ai import it.
- [high/redundant] 'Engage enemy: set target + commit window + FIGHTING/MOVING' block copy-pasted ~9x in defense.py — ai/behaviors/defense.py:33-46, ai/behaviors/defense.py:85-97 -> Introduce two tiny helpers in defense.py (or a shared combat_targeting.py): `_commit_ms(now)` and `engage(hero, enemy, now_ms)` which sets target, commit window
- [medium/redundant] Four LLM providers are near-identical SDK wrappers; Grok duplicates OpenAI almost verbatim — ai/providers/openai_provider.py:1-80, ai/providers/grok_provider.py:1-67 -> Collapse Grok into OpenAIProvider via an optional base_url param (or a 5-line subclass that only sets base_url+model). Pull the shared init/availability scaffol
- [medium/oversized_file] mock_provider.py (519 LoC) conflates four unrelated mock responders and an ad-hoc prompt sniffer — ai/providers/mock_provider.py:54-109, ai/providers/mock_provider.py:111-193 -> Split into a package ai/providers/mock/: dispatcher.py (complete() + format detection), autonomous.py, direct_prompt.py, conversation.py, legacy_decision.py. Th
- [medium/stringly_typed] hero.target stays a stringly-typed dict even though TargetType/HeroTask exist; arrival dispatch round-trips through strings — ai/contracts.py:122-129, ai/behaviors/bounty_pursuit.py:211-218 -> Make TargetType the authoring vocabulary without changing storage: replace every literal `hero.target = {'type': '...'}` with `hero.target = {'type': TargetType
- [medium/poor_design] defense.start_retreat picks the FIRST castle/marketplace in list order (not nearest) and leaves a stale hero.target — ai/behaviors/defense.py:269-277, tests/test_ai_defense.py:84-94 -> Select the nearest safe building by distance, clear hero.target, and reuse route_to_building (finding #4). Honor the same castle/inn/home 'safety' set used by a
- [low/redundant] Compass-from-vector logic duplicated (8-point vs 4-point) across poi_awareness and arrival_handlers — ai/behaviors/poi_awareness.py:78-86, ai/arrival_handlers.py:51-56 -> Put one `compass8(dx,dy)` and a `compass4(dx,dy)` (derived) in a shared geometry helper (game/sim/direction.py already hosts direction concepts via resolve_expl
- [low/over_complex] hunger.maybe_apply_meal_before_llm_action has redundant/contradictory branch logic — ai/behaviors/hunger.py:220-228 -> Collapse to: gate once on action membership, then delegate to tick_meal_hunger (which already self-guards). Drop the redundant should_seek_meal pre-check.
- [low/perf] Per-tick recompute of frontier scan + zone assignment with no memoization — ai/behaviors/exploration.py:67-133, ai/behaviors/exploration.py:31-64 -> Two cheap wins: (a) cache the most recent frontier-candidates list per hero keyed by (hero_gx,hero_gy,sim_now_ms bucketed to the commit window) so repeated call
- [low/boundary_leak] defense.py inconsistently normalizes building_type (.value vs raw) revealing a leaky enum/string boundary — ai/behaviors/defense.py:136-139, ai/behaviors/hunger.py:39 -> Pick one convention: always compare against BuildingType members (import the enum) — never against raw string literals — and delete the defensive .value unwrap.
- [low/redundant] bounty_pursuit duplicates 'compute goal_x/goal_y from bounty' three times — ai/behaviors/bounty_pursuit.py:195-197, ai/behaviors/bounty_pursuit.py:259-261 -> Add one helper `bounty_goal_xy(bounty, buildings, fallback_xy) -> tuple[float,float]` and use it in all three places. This also centralizes the get_goal_positio

## ui-hud (9)
- [critical/oversized_file] HUD is a 2477-line God-object mixing 7 responsibilities; split into a package — game/ui/hud.py:92 (class HUD), game/ui/hud.py:2000-2226 (render) -> Convert game/ui/hud.py into a package game/ui/hud/ where `HUD` becomes a thin coordinator (~250 LOC) that owns the sub-renderers/controllers and forwards. Extra
- [high/over_complex] render() is a 226-line monolith doing memorial-detection, layout, paint, and an inline command bar — game/ui/hud.py:2000-2226 -> (1) Move memorial-record construction out of render into a `_update_memorial_state(profiles)` step (or better, into ToastManager/MemorialController). (2) Extrac
- [high/stringly_typed] POI type->color palette duplicated verbatim; interaction_type is a stringly-typed enum scattered across 4 sites — game/ui/hud.py:549-557 (_type_colors in toast), game/ui/hud.py:1630-1638 (_poi_colors in minimap) -> Introduce a single source of truth. Minimum: a module-level constant POI_TYPE_COLORS in a shared module (e.g. game/ui/poi_style.py or alongside POIDefinition) i
- [high/boundary_leak] Pervasive getattr-with-default on the project's OWN typed entities; raw game_state dict hides real types — game/ui/hud.py:1393-1397 (vitals/prog/idn via getattr), game/ui/hud.py:1602-1613 (building x/y/grid_x/size via getattr) -> (1) Define a typed read-model the renderers consume — either a frozen `HudViewModel` dataclass assembled once by render_coordinator, or at minimum type the hot 
- [medium/redundant] Three near-identical toast systems with copy-pasted fade/blit/3-cap logic — game/ui/hud.py:383-425 (_render_wave_toast), game/ui/hud.py:536-609 (_render_poi_toasts) -> Extract a single `ToastManager` (see split blueprint) with one `Toast` dataclass (text, color, created_ms, duration_ms, optional countdown_end_ms, optional dot_
- [medium/perf] _ensure_radar_terrain_surface samples terrain pixel-by-pixel with set_at in a double loop (twice) — game/ui/hud.py:1522-1553 (two nested width x height loops with set_at + world_to_grid per pixel) -> (1) Merge the two loops into one — decide grass/water/mountain/tree in a single pass (tree is just another tile branch). (2) Use pygame.PixelArray or build a sm
- [medium/poor_design] handle_click is a 160-line linear hit-test wall returning stringly-typed action codes — game/ui/hud.py:2316-2476 -> (1) Collapse the duplicate sidebar branch to a single return. (2) Define an ordered hit-test as a list of (region_getter, handler) so order is data not 20 hand-
- [medium/boundary_leak] render_coordinator writes hud.watch_card_map_world_center/_wh — a render->ui back-channel for hit-testing — game/ui/hud.py:272-273 (decl), game/ui/hud.py:2402-2415 (read in handle_click to convert click->world) -> Encapsulate the watch-card map projection in one object owned by WatchCardRenderer: a small `MiniMapProjection(center, size, rect)` with `screen_to_world(x,y)` 
- [low/dead_code] Trivial pass-through wrappers and a no-op compat method add indirection — game/ui/hud.py:1493-1500 (_render_watch_card_chrome just calls _render_card_slot), game/ui/hud.py:1475-1491 (_render_card_slot resets fields then calls _render_hero_watch_card_infocard) -> Fold _render_card_slot's field resets into the renderer and delete _render_watch_card_chrome (call the renderer directly). Remove toggle_right_panel and the dea

## ui-panels (7)
- [high/oversized_file] _render_standard_hero is a 535-line god-method that hand-codes a vertical layout with 47 inline TextLabel.render calls and 12 magic y+=14 increments — game/ui/hero_panel.py:398-933 -> Split into a HeroSheetModel extractor + a small scrolling layout helper, then express the sheet as data. (1) Move the field-merge logic (416-529) into hero_prof
- [high/redundant] BuildingListPanel and BuildCatalogPanel are two live, near-duplicate 'pick a building' UIs with copy-pasted hotkeys, building list, and availability logic — game/ui/building_list_panel.py:16-27, game/ui/build_catalog_panel.py:21-32 -> Extract one shared module game/ui/build_catalog_model.py owning: PLACEABLE_BUILDINGS, BUILDING_HOTKEYS, and `building_availability(building_type, economy, build
- [high/boundary_leak] HeroPanel re-derives hero intent and last-decision that the sim already computes, via deep getattr/try-except fallback chains (sim->ui boundary leak) — game/ui/hero_panel.py:97-132, game/ui/hero_panel.py:134-207 -> Make the panel a pure consumer of one typed snapshot. When hero_profile is present, read v.current_intent/current_state/last_decision only (already done partial
- [medium/redundant] building_type normalization is centralized in normalize_building_type_key() but re-implemented inline in two renderers and skipped entirely in a third — game/ui/building_renderers/__init__.py:149-155, game/ui/building_renderers/economic_panel.py:345-349 -> Import normalize_building_type_key into all three renderer modules and replace the inline blocks / the bare str() with a single call. Since the dispatch in get_
- [medium/redundant] Button render boilerplate (full color/border/hover block) is copy-pasted ~6 times across renderers and BuildingPanel — game/ui/building_renderers/economic_panel.py:92-112, game/ui/building_renderers/economic_panel.py:218-244 -> Add one helper to BuildingPanel (or building_renderers/__init__.py): `draw_action_button(panel, surface, y, text, *, hovered, enabled, scheme='primary') -> (new
- [medium/poor_design] Domain renderers mutate BuildingPanel hit-rects as a side effect instead of returning them (bidirectional coupling) — game/ui/building_panel.py:300-304, game/ui/building_renderers/economic_panel.py:106-111 -> Change BuildingDomainRenderer.render to return a small result object instead of mutating the panel: `RenderResult(y: int, hit_rects: dict[str, pygame.Rect], res
- [medium/over_complex] BuildingPanel._apply_blacksmith_research is a nested TypeError-retry ladder probing 4 possible building APIs — game/ui/building_panel.py:392-426 -> Standardize the Blacksmith research entrypoint to a single method `research(key: str, economy, game_state) -> bool` on the building class (add a thin adapter on

## ui-other (game/ui non-HUD, non-panel modules: pause_menu, chat_panel, widgets, dev_tools_panel, command_bar, top_bar, speed_control, building_menu, theme, info_card, memorial_card, demolish_confirm_overlay, building_interior_overlay, micro_view_manager, pin_alert_watcher, pin_slot, ui_actions) (12)
- [high/oversized_file] pause_menu.py: 811-LOC god-class mixing router + 5 pages + 2 dialogs + audio adapter — game/ui/pause_menu.py:15-811 -> Introduce a tiny MenuPage protocol (handle_click(pos)->str|None, render(surface, panel_rect, mouse)) and move each page into its own module under game/ui/pause/
- [high/redundant] pause_menu: button geometry computed twice (handle_click vs render) and must be hand-synced — game/ui/pause_menu.py:331-372, game/ui/pause_menu.py:652-724 -> Compute each page's hit-rects once and reuse for both draw and hit-test. Simplest: have render() store the rects it drew (self._difficulty_level_rects, self._di
- [high/redundant] chat_panel: render() and render_watch_band() duplicate ~90 lines of message/scroll/input logic — game/ui/chat_panel.py:229-408, game/ui/chat_panel.py:410-548 -> Extract two private helpers used by both renderers: _draw_message_log(surface, msg_rect, *, pad, line_h, show_placeholder_stripe) and _draw_input_box(surface, i
- [high/perf] Per-frame SysFont allocation in modal overlays (memorial / demolish / interior) — game/ui/memorial_card.py:97-100, game/ui/demolish_confirm_overlay.py:56-58 -> Create the fonts once. For memorial_card/demolish_confirm (no theme today), build them in __init__ and store as instance attrs (they want a serif look so keep S
- [medium/redundant] chat_panel: two keydown handlers duplicate text-edit + 200-char clamp logic — game/ui/chat_panel.py:559-579, game/ui/chat_panel.py:581-614 -> Factor the editing operations into intent methods (_edit_backspace, _edit_insert(ch), _try_send()->str|None) and have both handlers translate their event into o
- [medium/redundant] Three modal overlays reimplement the same dim-backdrop + centered-card + close-button shell — game/ui/memorial_card.py:73-176, game/ui/demolish_confirm_overlay.py:39-112 -> Add a ModalOverlay base (or a free function draw_modal_shell) in widgets.py that owns the cached dim surface, centered card rect, and a draw_button(rect,label,h
- [medium/boundary_leak] micro_view_manager couples to HUD/panels via getattr/hasattr duck-typing and private HUD methods — game/ui/micro_view_manager.py:107-164, game/ui/micro_view_manager.py:119 -> Define small Protocols (ChatPanelLike with is_active()/render()/handle_click(); RightPanelHost with render_overview()/render_hero_focus_profile()/right_panel_to
- [medium/redundant] dev_tools_panel reimplements word-wrap and inlines scroll math 4x; events stringly-typed dicts — game/ui/dev_tools_panel.py:72-92, game/ui/dev_tools_panel.py:111-132 -> Reuse the shared wrap helper (see duplication item). Extract a single _scroll_metrics()->(line_h,content_h,num_visible,max_scroll) used everywhere. Make a tiny 
- [medium/redundant] word-wrap implemented 3 times across the area (and more across game/ui) — game/ui/chat_panel.py:102-132, game/ui/dev_tools_panel.py:72-92 -> Create game/ui/text_wrap.py with a single wrap_text(font, text, max_width, *, break_long_words=True, honor_newlines=False) -> list[str] covering both behaviors.
- [low/redundant] Button render call repeats ~15 styling kwargs at every call site — game/ui/pause_menu.py:464-483, game/ui/command_bar.py:108-132 -> Introduce a ButtonStyle dataclass (or a couple of module constants in widgets.py: PRIMARY_BUTTON, DANGER_BUTTON) bundling the texture paths + palette, and add B
- [low/perf] speed_control re-derives active tier index every frame; SPEED_TIERS/SPEED_SYMBOLS positional coupling — game/ui/speed_control.py:97-102, game/ui/speed_control.py:25-28 -> Define one ordered table of tiers: TIERS = [(multiplier, symbol, name), ...] (or reuse SPEED_TIER_NAMES keys) so symbol/label can't desync. Factor the 'multipli
- [low/boundary_leak] pause_menu render() forces a full Ursina HUD texture re-upload via setattr side-effect — game/ui/pause_menu.py:434-435 -> Expose a named method on the engine/viewer (e.g. engine.request_hud_full_upload()) and call that. Better, have the HUD compositor decide when a modal is visible

## gfx-ursina-renderer (12)
- [high/oversized_file] ursina_renderer.py is a 1985-LOC god-module mixing 6 unrelated responsibilities — game/graphics/ursina_renderer.py:1-1985 -> Split into the focused modules in oversized_files below. Keep UrsinaRenderer as a thin orchestrator (__init__ + update() pipeline + _destroy_removed_entities). 
- [high/dead_code] Entire underground rendering subsystem is dead (~250 LOC behind an early return) — game/graphics/ursina_renderer.py:1380-1441, game/graphics/ursina_renderer.py:1320-1378 -> Extract the whole underground concern into game/graphics/ursina_underground.py as an UrsinaUndergroundCollab (mesh creation, stalactites, torch PointLights, lay
- [high/boundary_leak] UrsinaTerrainFogCollab reaches into 12+ private renderer fields 107 times — broken ownership boundary — game/graphics/ursina_terrain_fog_collab.py:1-1783, game/graphics/ursina_renderer.py:451-472 -> Move terrain/fog state ONTO UrsinaTerrainFogCollab (it already owns the methods). Give it its own __init__ fields and have UrsinaRenderer delegate the ~5 public
- [medium/dead_code] Dead method _unit_anim_surface (~72 LOC) — superseded by atlas anim path — game/graphics/ursina_renderer.py:543-614 -> Delete _unit_anim_surface entirely. Update the three comments to reference _compute_anim_frame instead. The two anim engines collapse to one.
- [medium/redundant] Cave/mine-entrance tint block is triplicated across the 3 building render paths — game/graphics/ursina_renderer.py:1209-1217, game/graphics/ursina_renderer.py:1244-1252 -> Extract a module-level free function _apply_cave_entrance_tint(ent, b) and call it once after the if/elif/else that picks the entity, instead of inside each bra
- [medium/redundant] ~10x copy-pasted cull/layer-gate/re-enable boilerplate across the 5 unit _sync_snapshot_* methods — game/graphics/ursina_renderer.py:1093-1100, game/graphics/ursina_renderer.py:1133-1144 -> Add one helper that returns 'visible' or hides+marks-active in one shot, e.g. _begin_unit(self, obj, *, layer, active_ids) -> Entity|None. It centralizes layer 
- [medium/stringly_typed] is_lair detected by hasattr(building,'stash_gold') in 5 places — stringly/structurally-typed dispatch — game/graphics/ursina_renderer.py:1148, game/graphics/ursina_renderer.py:359 -> Rely on the explicit is_lair flag that already exists on the sim object (prefabs.py already reads getattr(building,'is_lair',False)). Make is_lair authoritative
- [low/redundant] Five env scale constants duplicated between renderer and ursina_environment; the renderer copies are dead — game/graphics/ursina_renderer.py:82-91, game/graphics/ursina_environment.py:15-19 -> Delete lines 82-91 from ursina_renderer.py. If anything in the renderer needs them later, import from ursina_environment (single source of truth). TERRAIN_SCALE
- [low/dead_code] _apply_poi_mystery_state is a documented no-op stub still wired into all 3 building paths — game/graphics/ursina_renderer.py:1071-1074, game/graphics/ursina_renderer.py:1219 -> Delete the method and its 3 call sites. POI visibility is already handled inline (1119-1132).
- [low/perf] Per-frame function-local imports in hot sync methods — game/graphics/ursina_renderer.py:1830, game/graphics/ursina_renderer.py:1903-1904 -> Hoist 'from ursina import destroy' (and 'import random') to module top and use the bound name. Removes ~5 in-loop import statements.
- [low/over_complex] KINGDOM_URSINA_INSTANCING branch duplicates the update() pipeline tail and early-returns — game/graphics/ursina_renderer.py:1018-1031 -> Either delete the instancing branch (project memory says it has known visual issues and is opt-in/dormant) OR factor the shared head (shadow bounds, blend, visi
- [low/poor_design] Tax-overlay public API + building world-space UI live in the renderer module (wrong home) — game/graphics/ursina_renderer.py:156-367 -> Move these into game/graphics/ursina_building_ui.py (alongside the existing ursina_unit_overlays.py). Re-export the 3 public names from ursina_renderer for back

## gfx-ursina-terrain-app (9)
- [high/oversized_file] UrsinaTerrainFogCollab is a 1783-LOC god-class fusing 7 responsibilities behind ~70 reach-ins into renderer privates — game/graphics/ursina_terrain_fog_collab.py:127, game/graphics/ursina_terrain_fog_collab.py:202 -> Split into 5 focused modules under game/graphics/terrain/, each taking the renderer (or a small explicit state object) in its constructor. Move the renderer-own
- [high/oversized_file] UrsinaApp.update() is a ~250-line per-frame closure mixing 8 concerns; impossible to unit-test or profile in isolation — game/graphics/ursina_app.py:1274, game/graphics/ursina_app.py:1277 -> Extract the per-frame body into named methods on UrsinaApp so update() becomes a ~25-line orchestrator, and pull all camera-rig behavior into a dedicated Camera
- [high/redundant] Tree-entity creation duplicated verbatim between build_3d_terrain and sync_dynamic_trees (instanced + legacy branches both copied) — game/graphics/ursina_terrain_fog_collab.py:1115, game/graphics/ursina_terrain_fog_collab.py:1160 -> Extract a single _create_tree_at(root, tx, ty, *, world=None) helper that returns the entity-or-stub and does both the instanced and legacy branches plus chunk 
- [medium/dead_code] update_cave_entrance_shader is dead code: an unconditional early return hides ~40 lines behind a feature gate — game/graphics/ursina_terrain_fog_collab.py:1440, game/graphics/ursina_terrain_fog_collab.py:1448 -> Collapse to a documented one-liner and delete the unreachable block; if the underground-hole feature is genuinely shelved, also remove the renderer call site an
- [medium/poor_design] Camera-layer state is mirrored in UrsinaApp and copied into the renderer every frame (stringly/duplicated source of truth) — game/graphics/ursina_app.py:126, game/graphics/ursina_app.py:1507 -> Make UrsinaApp the single owner and have the renderer pull it through the snapshot or a getter, OR (smaller) define an IntEnum CameraLayer(SURFACE=0, UNDERGROUN
- [medium/over_complex] _setup_ursina_camera_for_castle (175 LOC) is dominated by debug-only env-var branches interleaved with production framing — game/graphics/ursina_app.py:317, game/graphics/ursina_app.py:342 -> Extract the debug-camera overrides into a separate helper (or the new CameraController) gated by a single 'is any debug-cam env set' check, leaving _setup_ursin
- [medium/error_handling] Pervasive try/except Exception (pass) and getattr-as-API obscure real errors across both big files — game/graphics/ursina_terrain_fog_collab.py:115, game/graphics/ursina_terrain_fog_collab.py:453 -> Two passes: (1) Replace getattr(self._r, '_x', default) with direct self._r._x (or state.x after the TerrainRenderState extraction) wherever the field is uncond
- [low/redundant] grass tiles_per_repeat env parse copy-pasted 3x with identical fallback logic — game/graphics/ursina_terrain_fog_collab.py:1342, game/graphics/ursina_terrain_fog_collab.py:1514 -> Add one helper (e.g. in game/graphics/ursina_environment.py or a terrain config module): def ground_tiles_per_repeat() -> float. Call it from all three sites. T
- [low/over_complex] HUD GPU upload (_refresh_ui_overlay_texture, 127 LOC) embeds a hand-rolled dirty-row diff that belongs in its own uploader — game/graphics/ursina_app.py:837, game/graphics/ursina_app.py:913 -> Extract a HudTextureUploader class (own file) holding composite_texture/size, prev_raw, quick_sig, and exposing upload(surf, force) + invalidate(). This isolate

## gfx-instanced-sprites (10)
- [high/oversized_file] instanced_nature_renderer.py (1009 LOC) mixes GLSL, GLB color-extraction, render-state, and per-frame upload in one file — game/graphics/instanced_nature_renderer.py:1-1009 -> Split into focused modules under a game/graphics/nature/ package per the blueprint in oversized_files. Move the shader to nature/nature_shader.py, all GLB color
- [high/stringly_typed] Per-model instance state is a raw dict of ~12 parallel arrays with magic string keys — game/graphics/instanced_nature_renderer.py:581-603, game/graphics/instanced_nature_renderer.py:452 -> Introduce a @dataclass PerModelInstances with __slots__ holding the GeomNode refs + the SoA arrays, and a small alloc()/free()/set_scale() API that owns the fre
- [high/redundant] GLB base-color extraction is triplicated and over-complicated within the nature renderer — game/graphics/instanced_nature_renderer.py:236-302, game/graphics/instanced_nature_renderer.py:320-354 -> Delete the base_color return path from _load_geom_node_for_model entirely (return just (geom_node, source_np)). Keep only _resolve_geom_color as the single colo
- [high/redundant] Animation clip/frame state machine duplicated verbatim between instanced and legacy unit renderers — game/graphics/instanced_unit_renderer.py:214-271, game/graphics/ursina_renderer.py:543-614 -> Extract the FSM into ursina_units_anim.py (which already owns the base_clip functions and _frame_index_for_clip) as a free function resolve_anim_clip_frame(stat
- [high/oversized_file] interior_sprites.py (855 LOC): one class with 19 hardcoded layout methods and a re-derived `wall_h = h // 3` everywhere — game/graphics/interior_sprites.py:69-856, game/graphics/interior_sprites.py:164 -> Split per the oversized_files blueprint into game/graphics/interiors/. Define a shared InteriorGeometry(w,h) helper (wall_h, floor_y, etc.) used by every genera
- [medium/redundant] The three unit *SpriteLibrary modules duplicate the load/cache/procedural skeleton — game/graphics/hero_sprites.py:24-92, game/graphics/enemy_sprites.py:19-88 -> Add a small SpriteLibraryBase (or a build_clips(kind, asset_subdir, actions, color, procedural_fn, size) free function in animation.py / a new sprite_library_ba
- [medium/boundary_leak] Tree growth crosses the render boundary via an Entity-mimicking stub with a callback through a property setter — game/graphics/ursina_terrain_fog_collab.py:44-124, game/graphics/ursina_terrain_fog_collab.py:104-116 -> Have sync_dynamic_trees branch on the instanced path and call renderer.update_tree_scale(iid, s) / remove_tree(iid) directly using the (tx,ty)->instance_id map 
- [medium/perf] update_buffer re-packs and GPU-reloads on every dirty model even when nothing in the visible set changed — game/graphics/instanced_nature_renderer.py:719-744, game/graphics/instanced_nature_renderer.py:889-964 -> Track the last visible-chunk set per model (or globally) and only mark a model dirty if the set of its instances that pass the (fog AND chunk) filter actually c
- [low/stringly_typed] Unit scale constants and snapshot access are stringly/duck-typed; per-iteration getattr in the hot pack loop — game/graphics/instanced_unit_renderer.py:340-451, game/graphics/instanced_unit_renderer.py:347 -> Once the snapshot dataclasses are stable (they are, post-WK63), replace getattr-with-default by direct typed attribute access for the required fields and reserv
- [low/perf] _bake_per_geom_material_to_vertex_colors fills the color column one vertex at a time in Python — game/graphics/instanced_nature_renderer.py:405-411 -> If the single-uniform-per-model approach is acceptable (it already binds u_modelColor=white and bakes flat per-geom colors), prefer setting the geom render-stat

## gfx-pygame-vfx (game/graphics/renderers/*, game/graphics/vfx.py, animation.py, pygame_renderer.py) (11)
- [high/poor_design] All guards share ONE renderer/animation instance (lockstep animation bug) — game/graphics/renderers/registry.py:24, game/graphics/renderers/registry.py:93-95 -> Give guards the same per-entity treatment as peasants. Add a `_guard_renderers: dict[int, GuardRenderer]` and a `_guard_renderer_for(guard)` lazy factory; route
- [high/redundant] Identical one-shot animation FSM duplicated across 3 renderers (~50 lines each) — game/graphics/renderers/hero_renderer.py:54-99, game/graphics/renderers/enemy_renderer.py:44-80 -> Extract a single OneShotAnimController (or a small AnimatedEntityRenderer base) in a new game/graphics/renderers/_anim_base.py. It holds the AnimationPlayer + o
- [medium/redundant] Health-bar draw logic duplicated 4x; no shared overlay helper — game/graphics/renderers/hero_renderer.py:141-152, game/graphics/renderers/enemy_renderer.py:103-114 -> Add `draw_health_bar(surface, *, center_x, top_y, width, height, pct)` to a new game/graphics/renderers/_overlays.py (or font_cache's sibling). Each renderer co
- [medium/boundary_leak] _state_get Mapping shim copy-pasted in 3 files but renderers never receive a Mapping (false flexibility + leaky boundary) — game/graphics/renderers/hero_renderer.py:12-15, game/graphics/renderers/enemy_renderer.py:11-14 -> Define a single RenderState Protocol (or just a typed read view) in one module and import it; drop the Mapping branch. Either (a) commit to live-entity renderin
- [medium/dead_code] Dead code: set_one_shot() methods and _anim_lock_one_shot legacy-compat read path — game/graphics/renderers/hero_renderer.py:40-42, game/graphics/renderers/enemy_renderer.py:31-33 -> Delete set_one_shot from hero/enemy renderers. Delete the `_anim_lock_one_shot` fallback read in all three update_animation methods. If extracting the OneShotAn
- [medium/poor_design] RendererRegistry keys renderers by id(entity), contradicting WK63 stable entity_id/hero_id — game/graphics/renderers/registry.py:28-30, game/graphics/renderers/registry.py:32-70 -> Switch _key to use the stable id: `getattr(entity, 'hero_id', None) or getattr(entity, 'entity_id', None) or id(entity)` as a transitional step, then to pure en
- [medium/over_complex] VFXSystem.render() is a 110-line god-method with repeated debris-footprint branching and an unreachable size_px==1 path — game/graphics/vfx.py:300-412, game/graphics/vfx.py:335-358 -> Split render() into _render_particles / _render_projectiles / _render_debris private methods. Compute the projectile unit-direction once at spawn (store dx_norm
- [medium/stringly_typed] VFX event ingestion is stringly-typed over raw dicts with no schema — game/graphics/vfx.py:79-129, game/graphics/vfx.py:93-110 -> Introduce a small typed VFX event layer (Enum VFXEventType + frozen dataclasses or a TypedDict per kind) in game/graphics/vfx_events.py, and have _emit_event br
- [low/redundant] PeasantRenderer/TaxCollectorRenderer/GuardRenderer subclasses add nothing but a hardcoded worker_type — game/graphics/renderers/worker_renderer.py:256-275, game/graphics/renderers/registry.py:13 -> Have RendererRegistry construct WorkerRenderer(worker_id=..., worker_type='peasant'|'tax_collector'|'guard', size_px=...) directly and delete the three wrapper 
- [low/dead_code] Enemy fallback-triangle render is dead and ordered after a guaranteed-non-None deref — game/graphics/renderers/enemy_renderer.py:97, game/graphics/renderers/enemy_renderer.py:116-123 -> Delete enemy_renderer.py:116-123. (Optionally hoist the health-bar draw, currently lines 103-114, after the blit — it already is.) For worker_renderer the None-
- [low/boundary_leak] BountyRenderer caches UI surfaces by writing 9 _ui_cache_* attributes onto sim bounty objects — game/graphics/renderers/bounty_renderer.py:52-103 -> Give BountyRenderer its own cache dict keyed by stable bounty id (WK63 gives entities ids; add bounty_id or use a stable bounty key). Store (reward_val -> surf)

## audio (9)
- [high/oversized_file] audio_system.py (533 LOC) mixes 5 unrelated responsibilities — split into a focused package — C:\Users\Jaimie Montague\OneDrive\Documents\Kingdom\game\audio\audio_system.py:1-533 -> Convert game/audio into a package with one responsibility per module and keep AudioSystem as a thin facade so engine.py's call sites (on_event, set_listener_vie
- [high/boundary_leak] Enemy-sound distance attenuation uses the 2D camera, wrong in 3D (acknowledged but unfixed) — C:\Users\Jaimie Montague\OneDrive\Documents\Kingdom\game\audio\audio_system.py:190-200, C:\Users\Jaimie Montague\OneDrive\Documents\Kingdom\game\audio\audio_system.py:295-296 -> Introduce a single 'listener world position' input that both paths consume, and have the engine pass the ACTIVE camera (3D camera ground-projected position in U
- [medium/dead_code] Dead public method emit_from_events() — no caller anywhere — C:\Users\Jaimie Montague\OneDrive\Documents\Kingdom\game\audio\audio_system.py:234-252 -> Delete emit_from_events (lines 234-252). on_event is the live path. If a batch entry point is ever wanted, add it back as a one-liner that loops on_event.
- [medium/redundant] Duplicated SFX asset-discovery/preload logic in both audio files — C:\Users\Jaimie Montague\OneDrive\Documents\Kingdom\game\audio\audio_system.py:128-162, C:\Users\Jaimie Montague\OneDrive\Documents\Kingdom\game\audio\enemy_sounds.py:99-125 -> Extract a single SfxCache (see oversized-file blueprint, sfx_cache.py) with one documented extension order and have both callers use it. Pick ONE probe order (r
- [medium/poor_design] Two independent sound caps + shared 8-channel pool can starve ambient and each other — C:\Users\Jaimie Montague\OneDrive\Documents\Kingdom\game\audio\enemy_sounds.py:75-76, C:\Users\Jaimie Montague\OneDrive\Documents\Kingdom\game\audio\enemy_sounds.py:127-137 -> Set an explicit mixing-channel budget at init (e.g. pygame.mixer.set_num_channels(16)) and reserve a dedicated channel for ambient via pygame.mixer.set_reserved
- [medium/stringly_typed] Ambient cooldowns keyed by id(enemy) instead of stable entity_id; unbounded growth risk — C:\Users\Jaimie Montague\OneDrive\Documents\Kingdom\game\audio\enemy_sounds.py:93, C:\Users\Jaimie Montague\OneDrive\Documents\Kingdom\game\audio\enemy_sounds.py:259 -> Key cooldowns by getattr(enemy,'entity_id',id(enemy)) (prefer the stable id, fall back for safety). This makes cleanup correct and removes the recycled-id hazar
- [medium/boundary_leak] enemy_killed sound only fires when gold_recipients is non-empty — death SFX silently dropped — C:\Users\Jaimie Montague\OneDrive\Documents\Kingdom\game\systems\combat.py:204-227, C:\Users\Jaimie Montague\OneDrive\Documents\Kingdom\game\audio\audio_system.py:303-308 -> Hoist the ENEMY_KILLED emit out of the gold_recipients branch so it fires whenever `killed` is True, independent of gold distribution. (Cross-area: change lives
- [low/boundary_leak] EnemySoundManager reaches into AudioSystem private volume fields — C:\Users\Jaimie Montague\OneDrive\Documents\Kingdom\game\audio\enemy_sounds.py:181-182 -> Use the public getters, or (better, after the mixer split) pass a VolumeMixer into EnemySoundManager so it depends on an interface, not AudioSystem internals.
- [low/error_handling] _enemy_sounds attribute not created when mixer init fails — relies on short-circuit luck — C:\Users\Jaimie Montague\OneDrive\Documents\Kingdom\game\audio\audio_system.py:105-126, C:\Users\Jaimie Montague\OneDrive\Documents\Kingdom\game\audio\audio_system.py:291 -> Initialize all instance attributes (including self._enemy_sounds = None) at the very top of __init__, before any early return. This is also a prerequisite for t

## config-content (7)
- [high/redundant] Vestigial frozen-dataclass layer: 13 config classes whose instances are never used outside config.py — config.py:11-119 (WindowConfig..RangerConfig), config.py:220-242 (WINDOW=..,SIM=..,..,RANGER=..) -> Delete the unused dataclasses and instantiate-then-unpack churn. Keep the flat module-level constants (that is what 152 files import) as the public API, but def
- [high/oversized_file] config.py is a 774-LOC god-module mixing sim tuning, render constants, env/runtime, dev mode, and content registries — config.py:1-774, config.py:294-306 (COLOR_*) -> Convert config.py into a config/ package with a back-compat __init__.py that re-exports every current public symbol (so all ~152 importers keep working with zer
- [high/parallel_registry] Five parallel building registries keyed by the same strings; adding a building means editing 6 places — config.py:309 BUILDING_COSTS, config.py:362 BUILDING_SIZES -> Introduce ONE canonical BUILDING_DEFS registry keyed by BuildingType, holding a small frozen BuildingDef(size, color, cost, max_occupants, constraints, prereqs)
- [medium/dead_code] Zombie 'WK34 REMOVED' building content still fully defined and wired through 6 registries + factory — config.py:330-340 (gnome_hovel/elven_bungalow/dwarven_settlement/ballista_tower/wizard_tower/fairgrounds/library/royal_gardens costs=0 'REMOVED'), config.py:383-393 (their sizes) -> Decide per type: if truly cut, delete its entries from all building registries, from BuildingType, from BUILDING_CONSTRAINTS/PREREQUISITES, and from BuildingFac
- [medium/redundant] Seven copy-pasted env-var parse/clamp blocks with inconsistent patterns — config.py:598-602 (shadows flag), config.py:605-607 (shadow map size) -> Add typed helpers in config/env.py and replace all 7 blocks with one-liners. This also enforces consistent clamping and makes the determinism story explicit (al
- [medium/poor_design] DEV_MODE rebinds module-level constants in place (import-order fragile, hidden global mutation) — config.py:768-773 -> Move dev overrides into an explicit function apply_dev_overrides() in config/runtime.py that is called once from the game bootstrap (main.py / engine init), OR 
- [low/naming] Stale window title/version metadata hardcoded in two fields — config.py:16-17 (prototype_version='1.5.8'; game_title embeds 'v1.5.8' + a sentence-long changelog) -> Store the version once and compose the title from it. Drop the embedded changelog from the window title (put release notes in a CHANGELOG, not the OS titlebar).

## tools-qa (observe/QA/screenshot/validate/determinism stack) (12)
- [high/redundant] screenshot_scenarios.py: 17x copy-pasted scene-setup boilerplate (find/create castle, clear entities, reveal fog, anchor camera) — tools/screenshot_scenarios.py:192-194 (scenario_building_catalog preamble), tools/screenshot_scenarios.py:204-209 (castle find-or-create block, repeated 17x) -> Extract a single setup helper and a castle accessor, then have every scenario call them. This alone removes ~150-200 lines and makes the catalog uniform.
- [high/oversized_file] observe_sync.py: monolithic ~570-line main() mixing world setup, sim loop, metrics, and QA assertions — tools/observe_sync.py:268-840 (single main() function), tools/observe_sync.py:472-534 (inline QA-state init + nested helpers _hero_intent_label/_bounty_responder_count) -> Split into a setup builder, a small set of metric collector classes, and a verdict function; keep main() as orchestration only. See oversized_files for the full
- [high/poor_design] observe_sync.py: dual sim-clock — metrics use a SEPARATE time formula than the sim, breaking max_stuck_ms under speed multiplier — tools/observe_sync.py:537 (set_sim_now_ms(int(t * sim_dt * 1000)) — sim clock, scaled by multiplier), tools/observe_sync.py:582 (now_ms_val = int((t * 1000) / 60) — independent clock, ignores multiplier) -> Delete now_ms_val entirely and read the single authoritative clock via game.sim.timebase.now_ms() (already imported pattern). This aligns with WK62's single-own
- [high/error_handling] determinism_guard.py: unhandled ValueError crash when --paths points outside PROJECT_ROOT — tools/determinism_guard.py:106 (_violation: file.relative_to(PROJECT_ROOT)), tools/determinism_guard.py:126 (parse_error branch: file_path.relative_to(PROJECT_ROOT)) -> Compute the display path defensively with a helper that falls back to the raw path string when relative_to fails, and resolve input roots to absolute before sca
- [medium/parallel_registry] screenshot_scenarios.py: hand-maintained 21-branch get_scenario() dispatcher (parallel registry) — tools/screenshot_scenarios.py:1563-1601 (get_scenario if/elif chain), tools/screenshot_scenarios.py:185-1560 (the 18 scenario_* definitions it must mirror) -> Replace the if-ladder with a declarative dict mapping canonical name + aliases -> builder. Keep the public get_scenario signature identical (callers in capture_
- [medium/stringly_typed] observe_sync.py: hero/bounty contracts read entirely via getattr/hasattr probing instead of WK64 typed contracts — tools/observe_sync.py:75-85 (hero_target_label inspects dict/enemy_type/building_type by attribute presence), tools/observe_sync.py:493-533 (_hero_intent_label / _bounty_responder_count: hasattr('intent'), hasattr('responders'), hasattr('responder_count')) -> Once the entity contracts are confirmed present (they are post-WK64), delete the inference fallbacks and read concrete fields directly; assert presence rather t
- [medium/poor_design] determinism_guard.py: parse_error conflated with determinism violations (same nonzero exit, same FAIL header) — tools/determinism_guard.py:122-131 (parse_error appended into the same findings list), tools/determinism_guard.py:228-235 (main treats any finding as a violation, exit 1) -> Separate parse errors from violations: collect them in their own list, print under a distinct header, and (optionally) choose whether they should gate. At minim
- [medium/boundary_leak] capture_screenshots.py: reaches into GameEngine private surfaces (sim/render boundary leak) — tools/capture_screenshots.py:65-79 (_configure_engine_surface assigns engine._scaled_surface, engine._pause_overlay, engine._view_surface, engine._view_surface_size), game/engine.py:154-197 (the same private surfaces are owned/initialized by GameEngine) -> Add/define a public GameEngine.resize_surface(width, height) (or reuse the existing resize path the engine uses for window changes) that owns surface re-creatio
- [medium/other] No test coverage for any of the six QA/tooling scripts — tests/ (grep for screenshot_scenarios|determinism_guard|validate_assets|capture_screenshots|observe_sync|get_scenario|scan_file|Finding => 0 matches) -> Before refactoring the big files, add focused unit tests so the splits are safe: (1) determinism_guard.scan_file on small source snippets (positive: random.rand
- [low/poor_design] determinism_guard.py: AST matcher misses aliased imports (import time as t / from random import random) — tools/determinism_guard.py:89-101 (_attr_chain matches literal dotted names only), tools/determinism_guard.py:144-201 (chain == ['pygame','time','get_ticks'], chain[0]=='time', chain[0]=='random' literal checks) -> Resolve import aliases during the walk: collect `ast.Import`/`ast.ImportFrom` bindings into an alias map, then normalize the attribute chain before matching. Th
- [low/redundant] observe_sync.py: scenario dispatch split between argparse choices, an if-chain, and a nested _scenario_setup — tools/observe_sync.py:281-295 (argparse choices list of scenario names), tools/observe_sync.py:309-310 (direct_prompt_integration special-cased early-return) -> When extracting the observe_sync modules (see oversized_files), centralize scenarios into one registry mapping name -> a setup callable, and derive the argparse
- [low/naming] validate_assets.py: inconsistent collection typing (list[str] | None vs Iterable[str]) and over-broad manifest except — tools/validate_assets.py:74 (kinds: Iterable[str]), tools/validate_assets.py:167-168 (sfx_files: list[str] | None vs ambient_files: Iterable[str] — two params, same concept, different types) -> Normalize all manifest-list params to one type (Iterable[str], treating absence as empty before the call). Narrow the manifest-load except to (OSError, json.JSO